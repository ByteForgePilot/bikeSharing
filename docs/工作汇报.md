# Bicycle Data Logger 工作汇报

## 项目概述

自行车数据采集 Android 应用，用于实时采集骑行过程中的多传感器数据，为后续振动分析、骑行姿态分析和环境噪声分析提供原始数据。

**技术栈**：Kotlin + Jetpack Compose + Coroutines  
**最低系统要求**：Android 8.0 (API 26)  
**当前版本**：1.2

---

## 已完成功能

### 1. 多传感器同步采集

| 传感器 | 采样率 | 说明 |
|--------|--------|------|
| 加速度计 | 100 Hz | 三轴加速度，Z轴用于垂直振动分析 |
| GPS | 10 Hz | 经纬度、车速、航向角（支持网络定位辅助） |
| 陀螺仪 | 50 Hz | 偏航角速度，获取车把转动方向 |
| 麦克风 | 8 kHz | 16-bit PCM 单声道，用于音频特征提取 |

### 2. 数据存储

- 加速度计、GPS、陀螺仪数据合并写入 `传感器数据.txt`
- 麦克风数据单独存储（`音频.pcm` + `音频_时间戳.csv`）
- 纳秒级时间戳，跨传感器同步
- 每次采集生成独立的时间戳目录，文件管理器可直接访问
- 所有传感器数据保留 4 位小数精度

### 3. 用户界面（中文）

- Material 3 设计风格
- 实时状态显示（就绪 / 采集时长 / 错误）
- 传感器运行状态面板
- 采集计时器（时:分:秒）
- 保存路径显示
- 停止后可再次开始新采集
- 内置崩溃诊断界面

### 4. 前台服务

- 后台持续采集，不受界面退出影响
- 通知栏状态提示
- WakeLock 防休眠
- 适配 Android 14 前台服务类型限制

### 5. 权限管理

- 动态申请位置、麦克风权限
- 权限分关键/辅助两级，关键权限通过即可启动
- Android 9 以下申请存储权限
- Android 11+ 引导"所有文件访问"权限，失败自动回退应用目录
- Android 13+ 通知权限适配
- 权限被拒时中文提示引导用户

---

## 2026-05-20 更新内容

### Bug 修复

1. **权限流程重构** — 修复大部分手机点击开始无响应的问题
   - 原因：权限弹窗要求所有权限通过才启动，国产手机通知权限默认拒绝导致静默失败
   - 修复：分离关键权限（位置+麦克风）和辅助权限，关键权限通过即可启动
   - 增加 Toast 提示、防止多次点击、异常捕获

2. **Android 14 适配** — 修复前台服务启动闪退
   - 原因：缺少 `FOREGROUND_SERVICE_LOCATION` 和 `FOREGROUND_SERVICE_MICROPHONE` 权限声明
   - 修复：清单中新增两个权限，`startForeground()` 显式传入服务类型

3. **主线程 Looper 异常** — 修复 GPS 启动时 `Can't create handler inside thread` 崩溃
   - 原因：`LocationManager.requestLocationUpdates()` 需要在有 Looper 的线程调用
   - 修复：传感器 `start()` 调用通过 `withContext(Dispatchers.Main)` 确保在主线程

4. **数据精度优化** — 传感器浮点数统一保留 4 位小数

5. **资源管理** — 补充 `onDestroy()` 清理、AudioRecord 初始化检查、sessionPath 重置

6. **暂停/恢复采集** — 停止后服务保持存活，可立即再次开始新采集
   - 按钮文案改为「暂停采集」/「再次开始」
   - `doCleanup()` 不再销毁服务，消除重启延迟
   - `cleanedUp` 提前重置，避免快速启停时跳过清理

7. **自定义应用图标** — 支持用户替换图标，放入 `res/drawable/ic_launcher.jpg` 即可

8. **版本号** — 升级至 1.1 (versionCode 2)

---

## 2026-06-03 更新内容

### Bug 修复

1. **GPS 采样率提升至 10Hz** — `minTimeMs` 从 1000ms 改为 100ms
   - 界面显示同步更新

2. **新增网络定位辅助** — 解决 GPS 冷启动期间无数据问题
   - 同时注册 `GPS_PROVIDER`（10Hz 高精度）和 `NETWORK_PROVIDER`（5 秒间隔辅助）
   - 填补 GPS 冷启动 30~120 秒的真空期，录制即刻有位置数据

3. **暂停后无法停止采集** — 修复点击暂停后传感器继续运行
   - 根因：协程取消后 `finally` 块中 `withContext(Dispatchers.Main)` 检测到取消直接抛异常，`cleanupSession()` 从未执行
   - 修复：`withContext(NonCancellable + Dispatchers.Main)` 确保清理逻辑一定执行

4. **`Stream closed` 崩溃** — 修复暂停时 AudioCollector 重复 stop 导致 IO 异常
   - 根因：IO 线程和 Main 线程并发调用 `stop()`，无同步保护导致已关闭的 writer 被再次 flush
   - 修复：`stop()` 加 `synchronized` 锁 + 幂等性检查，`cleanupSession()` 中所有 stop 加 try-catch 兜底

5. **传感器 stop 线程安全** — `startXxxWriter()` 中 `collector.stop()` 切换至主线程执行
   - `SensorManager.unregisterListener()` 和 `LocationManager.removeUpdates()` 需要 Looper 线程
   - 所有 `stop()` 调用统一通过 `withContext(Dispatchers.Main)` 确保有效

---

## 待开发

1. 数据分析算法（MFCC、频谱平坦度、振动频谱）
2. 直线行驶检测（基于航向角）
3. 车轮转速推算
4. 实时数据波形可视化
5. 数据导出/分享
